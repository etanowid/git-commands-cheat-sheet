`git log -p go.mod | grep opentelemetry`
- This shows the commit history only for go.mod, displaying the changes made to it over time.
- The -p flag (short for --patch) shows the exact line-by-line changes.
